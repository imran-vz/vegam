# Require Sender Availability During Transfer

Production v1 Transfers require the Sender to keep Vegam open until the Receiver completes the Transfer. Vegam will not use cloud storage, relay-owned file hosting, or any background availability service in v1, so closing the Sender app ends the availability promise for that Transfer.
