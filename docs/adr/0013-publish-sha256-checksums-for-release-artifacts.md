# Publish SHA-256 Checksums for Release Artifacts

Production v1 publishes a SHA-256 checksum for every release artifact. The download experience must make each checksum copyable and provide a verify affordance so users can compare a downloaded artifact against the published checksum, which gives an unsigned macOS or Windows package a basic integrity-verification path without paid platform signing.
