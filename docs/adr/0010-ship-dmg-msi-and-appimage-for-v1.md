# Ship DMG, MSI, and AppImage for V1

Production v1 ships desktop packages as macOS `.dmg`, Windows `.msi`, and Linux `.AppImage`. Distro-native Linux packages such as `.deb` and `.rpm` remain future release work so the first desktop production pipeline stays focused on one artifact per supported operating system family.
